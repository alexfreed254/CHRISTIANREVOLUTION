import MediaLibrary from './MediaLibrary'

/** Sermons page — same library, focused label for simple navigation. */
export default function Sermons() {
  return (
    <MediaLibrary
      pageTitle="Sermons"
      pageSubtitle="Video and audio biblical teachings from CRM"
    />
  )
}
